import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import Button from '../../src/button.vue'

describe('Button.vue', () => {
  it('it is ok', () => {
    expect(1).to.eq(1)
  })
})
